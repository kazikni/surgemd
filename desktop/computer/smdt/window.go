package smdt

/*type WindowEvents struct {
	OnQuit   func()
	OnResize func(w, h int)
	OnMove   func(x, y int)
}

type Window interface {
	SetSize(uint, uint)
	GetSize() (uint, uint)

	SetTitle(string)
	GetTitle() string

	SetIcon(string)

	Fullscreen(bool)
	Borderless(bool)
	AlwaysOnTop(bool)

	Show()
	Hide()

	Run()
	Tick() bool
	Destroy()

	Events() *WindowEvents
	Handle() uintptr
}
*/
